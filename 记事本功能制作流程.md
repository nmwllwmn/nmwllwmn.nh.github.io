# 记事本功能制作流程

本文用于在后续“南洄市系列”网页解谜游戏中复用调查记事本功能。目标是让记事本像玩家自带的调查工具，而不是某个页面里的临时输入框。

## 1. 功能目标

- 记事本独立于具体页面存在，切换网页、URL 参数变化、进入不同 App 后内容都保留。
- 记事本默认以悬浮球形式存在，玩家随时能打开。
- 手机模拟器页面中，悬浮球应贴近手机外框，不要吸附到整个浏览器窗口边缘。
- 悬浮球可以拖拽，松手后自动吸附到最近边缘。
- 面板可以拖动和四角缩放，大小、位置、内容都自动保存。
- 面板内有一个固定区域展示“已记录线索”，由游戏状态自动生成。
- 记事本底部可以放“重新开始探索”，但必须二次确认；结局页的重开可以不确认。

## 2. 推荐数据结构

使用 `localStorage`，至少拆成两个 key：

```js
const NOTE_KEY = "game-investigation-notes-v1";
const NOTE_BOX_KEY = "game-investigation-note-box-v1";
```

`NOTE_KEY` 保存玩家手写内容：

```js
localStorage.setItem(NOTE_KEY, textarea.value);
```

`NOTE_BOX_KEY` 保存 UI 位置和尺寸：

```js
{
  "toggle": { "left": 1220, "top": 650 },
  "panel": { "left": 780, "top": 180, "width": 420, "height": 520 }
}
```

游戏进度另用独立 key 保存，例如：

```js
{
  "evidence": { "A": true, "B": true },
  "flags": { "foundB17": true, "sudokuSecretSolved": true }
}
```

不要把玩家手写笔记和游戏证据混在一个字段里。玩家笔记是玩家自己的，证据区是系统根据状态生成的。

## 3. 页面初始化规则

每个主要页面调用统一入口：

```js
initGameShell({ page: "phone-browser", stage: 10 });
```

在 `initGameShell` 内默认初始化记事本：

```js
function initGameShell(options = {}) {
  document.body.dataset.page = options.page || "";
  if (options.notebook !== false) initNotebook();
}
```

只有极特殊页面才传：

```js
initGameShell({ page: "cover", notebook: false });
```

## 4. DOM 结构

悬浮球：

```html
<button class="notebook-toggle" id="notebookToggle" type="button" aria-expanded="false">
  <span>记</span><strong>调查笔记</strong>
</button>
```

面板：

```html
<section class="notebook-panel" id="notebookPanel" hidden>
  <div class="notebook-head">
    <div>
      <h2>调查笔记</h2>
      <p class="muted">记录你觉得不对劲的碎片。内容自动保存在本机浏览器。</p>
    </div>
    <button id="notebookClose" type="button">收起</button>
  </div>

  <section class="notebook-clues">
    <h3>已记录线索</h3>
    <ul id="notebookEvidenceList"></ul>
  </section>

  <textarea id="notebookText" spellcheck="false"></textarea>

  <div class="notebook-actions">
    <span id="notebookStatus">已自动保存</span>
    <button id="notebookCopy" type="button">复制全部</button>
    <button id="notebookClear" type="button">清空</button>
  </div>

  <div class="notebook-restart">
    <button id="notebookRestart" type="button">重新开始探索</button>
  </div>

  <span class="notebook-resize-handle nw" data-corner="nw"></span>
  <span class="notebook-resize-handle ne" data-corner="ne"></span>
  <span class="notebook-resize-handle sw" data-corner="sw"></span>
  <span class="notebook-resize-handle se" data-corner="se"></span>
</section>
```

## 5. CSS 要点

悬浮球：

```css
.notebook-toggle {
  position: fixed;
  z-index: 141;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 52px;
  height: 52px;
  border-radius: 999px;
  cursor: grab;
  touch-action: none;
}
```

手机页面里隐藏文字，只保留圆形按钮：

```css
body[data-page^="phone"] .notebook-toggle strong {
  position: absolute;
  width: 1px;
  height: 1px;
  overflow: hidden;
  clip: rect(0 0 0 0);
}
```

面板：

```css
.notebook-panel {
  position: fixed;
  z-index: 140;
  display: flex;
  flex-direction: column;
  min-width: 280px;
  min-height: 320px;
  max-width: calc(100vw - 28px);
  max-height: calc(100vh - 24px);
  overflow: auto;
}
```

四角缩放点：

```css
.notebook-resize-handle {
  position: absolute;
  width: 16px;
  height: 16px;
  touch-action: none;
}

.notebook-resize-handle.nw { left: 4px; top: 4px; cursor: nwse-resize; }
.notebook-resize-handle.ne { right: 4px; top: 4px; cursor: nesw-resize; }
.notebook-resize-handle.sw { left: 4px; bottom: 4px; cursor: nesw-resize; }
.notebook-resize-handle.se { right: 4px; bottom: 4px; cursor: nwse-resize; }
```

## 6. 手机页吸附边界

不要直接用 `window.innerWidth` 作为吸附边界。宽屏电脑打开手机模拟器时，悬浮球会飞到浏览器最右边。

正确做法：如果页面存在 `.mi-phone`，用手机外框附近作为吸附区域。

```js
function getNotebookBounds() {
  const size = 52;
  const phone = document.querySelector(".mi-phone");
  const phoneRect = phone?.getBoundingClientRect();

  if (phoneRect && phoneRect.width > 0) {
    const margin = 14;
    return {
      left: Math.max(8, phoneRect.left - size - margin),
      right: Math.min(window.innerWidth - size - 8, phoneRect.right + margin),
      top: Math.max(8, phoneRect.top + 18),
      bottom: Math.min(window.innerHeight - size - 8, phoneRect.bottom - size - 18),
      phoneRect
    };
  }

  return {
    left: 8,
    right: window.innerWidth - size - 8,
    top: 8,
    bottom: window.innerHeight - size - 8,
    phoneRect: null
  };
}
```

默认位置建议放在手机右侧中下部：

```js
function getDefaultNotebookToggle() {
  const bounds = getNotebookBounds();
  if (bounds.phoneRect) {
    return {
      left: bounds.right,
      top: Math.min(Math.max(bounds.phoneRect.top + bounds.phoneRect.height * 0.58, bounds.top), bounds.bottom)
    };
  }
  return { left: bounds.right, top: bounds.bottom };
}
```

如果检测到旧位置离手机太远，自动重置：

```js
const farFromPhone = bounds.phoneRect && (
  saved.left > bounds.right + 24 ||
  saved.left + 52 < bounds.left - 24 ||
  saved.top > bounds.bottom + 24 ||
  saved.top + 52 < bounds.top - 24
);
```

## 7. 拖拽和吸附流程

1. `pointerdown` 记录起点。
2. `pointermove` 判断移动距离超过 4px 后进入拖拽。
3. 拖拽中不断调用 `clampNotebookToggle`，限制在边界内。
4. `pointerup` 调用 `snapNotebookToggle`，吸附到最近边缘。
5. 保存到 `NOTE_BOX_KEY`。
6. 拖拽结束后短暂屏蔽 click，避免拖完又打开面板。

```js
if (toggleDragged) {
  const rect = toggle.getBoundingClientRect();
  const snapped = snapNotebookToggle({ left: rect.left, top: rect.top });
  toggle.style.left = `${snapped.left}px`;
  toggle.style.top = `${snapped.top}px`;
  saveNotebookBox({ ...saved, toggle: snapped });
  suppressToggleClick = true;
  setTimeout(() => suppressToggleClick = false, 250);
}
```

## 8. 面板打开位置

点击悬浮球后，面板应出现在悬浮球附近，而不是固定右上角。

候选位置：

- 悬浮球右侧
- 悬浮球左侧
- 悬浮球下方
- 悬浮球上方

根据悬浮球更靠近哪条边选择优先方向，再用 `clampNotebookRect` 限制不出屏。

## 9. 已记录线索区域

线索区域不写玩家自由文本，而是读取游戏状态。

```js
const evidenceLabels = {
  A: "林舟 S05-S07 为 Web Import",
  B: "2023 旧路线含 S08",
  C: "2023 试跑人员记录与 S08 重合"
};
```

渲染：

```js
function renderNotebookEvidence() {
  const state = loadGame();
  evidenceList.innerHTML = Object.entries(evidenceLabels)
    .map(([key, label]) => `<li class="${state.evidence[key] ? "done" : ""}">${label}</li>`)
    .join("");
}
```

注意：系统线索区可以显示“已恢复/未恢复”，但不要替玩家解释答案。

## 10. 重开逻辑

记事本底部的重开按钮必须确认：

```js
if (!confirm("确定重新开始探索？调查进度会清空，调查笔记会保留。")) return;
restartExploration({ keepNotebook: true });
```

建议重开时保留玩家手写笔记，清空游戏进度：

```js
function restartExploration({ keepNotebook = true } = {}) {
  const note = keepNotebook ? localStorage.getItem(NOTE_KEY) : "";
  localStorage.removeItem(STORE_KEY);
  if (keepNotebook && note !== null) localStorage.setItem(NOTE_KEY, note);
  location.href = "index.html";
}
```

## 11. 验收清单

- 首页、手机桌面、微信、浏览器、地图、相册、结局页都能看到悬浮球。
- 宽屏电脑打开手机模拟器时，悬浮球贴近手机外框，不跑到浏览器最右边。
- 手机浏览器打开时，悬浮球不遮挡电量、返回按钮、底部导航。
- 输入笔记后刷新页面，内容仍在。
- 从 `phone-browser.html?site=...` 跳到另一个 URL，内容仍在。
- 拖动悬浮球后刷新，位置保留。
- 打开面板后，它出现在悬浮球附近。
- 四角缩放可用，刷新后尺寸保留。
- 已记录线索区随证据状态变化。
- 记事本重开需要确认，结局页重开不需要确认。
- 截图检查桌面 1366x768、宽屏 2048x1145、手机 390x844 三种视口。
